# Three.js Journey

## Setup
Download [Node.js](https://nodejs.org/en/download/).
Run this followed commands:

``` bash
# Install dependencies (only the first time)
npm install

# Run the local server at localhost:8080
npm run dev

# Build for production in the dist/ directory
npm run build
```

## TO-DO
- Fix the typeface in order to diminish the amount of polygons so that you can create many instances of the question marks.
- Animate an uppercase "WHO ARE YOU" that goes in and out of the lowercase one.
- Create a 3D chatbot.